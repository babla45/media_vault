package com.example.bib_vault.ui.theme

import androidx.compose.ui.graphics.Color

// ──────────────────────────────────────
//  BibVault Dark Theme Palette
//  A premium security-app aesthetic
//  with emerald green accents on deep charcoal
// ──────────────────────────────────────

// Primary — Emerald Green
val VaultPrimary = Color(0xFF10B981)
val VaultPrimaryLight = Color(0xFF34D399)
val VaultPrimaryDark = Color(0xFF059669)

// Secondary — Teal/Cyan
val VaultSecondary = Color(0xFF06B6D4)
val VaultSecondaryLight = Color(0xFF22D3EE)

// Surface — Deep Charcoal
val VaultSurfaceDark = Color(0xFF0F172A)
val VaultSurface = Color(0xFF1E293B)
val VaultSurfaceLight = Color(0xFF334155)
val VaultSurfaceCard = Color(0xFF1E293B)

// Background
val VaultBackground = Color(0xFF0B1120)

// On colors (text/icons)
val VaultOnPrimary = Color(0xFF002114)
val VaultOnSurface = Color(0xFFE2E8F0)
val VaultOnSurfaceVariant = Color(0xFF94A3B8)
val VaultOnBackground = Color(0xFFF1F5F9)

// Error
val VaultError = Color(0xFFEF4444)
val VaultOnError = Color(0xFFFFFFFF)

// Accent / highlight
val VaultAccentGold = Color(0xFFF59E0B)
val VaultAccentPurple = Color(0xFF8B5CF6)

// Media type colors
val VideoColor = Color(0xFFEF4444)     // Red for video
val AudioColor = Color(0xFF3B82F6)     // Blue for audio
val ImageColor = Color(0xFF10B981)     // Green for image

// Glassmorphism overlay
val GlassWhite = Color(0x1AFFFFFF)
val GlassBorder = Color(0x33FFFFFF)